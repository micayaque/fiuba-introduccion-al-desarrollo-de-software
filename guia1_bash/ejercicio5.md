### 5- Borre el directorio que generó en el pto.4

```bash
sudo rmdir /home/usuario #Falla si hay algo adentro

sudo rm -r /home/usuario #Borra el contenido interno

sudo rm -ri /home/usuario #Te pregunta antes de borrar cada archivo o subdirectorio
```
